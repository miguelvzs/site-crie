# Spec — Página Como Ajudar / Doe (`/doe`)

Status: draft. Página nova, não existia no site antigo.

## Blocos
1. **Doação PIX** (`#pix`) — chave/QR code pendente (`pendencias.md`). Renderiza placeholder explícito, nunca chave fake
2. **Voluntariado** (`#voluntariado`) — como participar
3. **Parcerias** (`#parcerias`) — empresas/organizações
4. **Transparência** (`#transparencia`) — prestação de contas, CNPJ. Link do footer aponta pra cá (`/doe#transparencia`)

## Critério de aceite
- [ ] Nunca renderizar chave PIX/QR code fake — placeholder explícito até dado real chegar
- [ ] `#transparencia` existe (footer depende do id)
- [ ] Heading order, Lighthouse A11y ≥95
